# MeoArc Frontend — biến môi trường
# Sao chép file này thành `.env.local` rồi điền giá trị khi cần.

# Base URL của backend thật (REST + SSE). Xem docs/02-API-CONTRACT.md.
# - Bỏ trống  → FE chạy hoàn toàn bằng dữ liệu mock (mặc định, dùng cho demo/SRS).
# - Có giá trị → FE gọi backend thật qua src/lib/api.ts (createHttpApi).
# Ví dụ: VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_API_BASE_URL=
