# File rỗng này (tên đặc biệt __init__.py) báo cho Python biết:
# "thư mục app/ là một GÓI (package) — được phép import như app.api.app".
# Không có nó, lệnh `from app.api.app import app` sẽ báo lỗi không tìm thấy.
