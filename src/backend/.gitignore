# Môi trường ảo + cache do Python/uv sinh ra — KHÔNG commit (giống node_modules bên FE)
.venv/
__pycache__/
*.pyc

# File biến môi trường chứa bí mật
.env
.env.local
.env.example
# Database SQLite cục bộ — dữ liệu máy bạn, không commit
meoarc.db

# CSDL tam khi chay thu trong moi truong gia lap CI (SQLite)
ci*.db
